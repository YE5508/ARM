# 天空块机构从板通信协议

本文档适用于天空块机构从板与主控板之间的 CAN 通信。主控板发送控制命令，天空块机构从板执行动作并回传状态或故障信息。

## 约定

- CAN ID 使用十六进制表示，`DLC` 为数据长度（字节）。
- `DATA0` 至 `DATA7` 按 CAN 数据区字节顺序排列。
- `M`：使能字段；`G`：抓取动作；`P`：放下动作；`A`：机械臂复位；`R`：复位/开关控制字段；`S`：命令参数或复位响应。
- `0=失能/关闭`，`1=使能/打开`。

## 主控板 -> 天空块机构从板

| 功能 | CANID | DLC | 数据区 | 从板动作 |
|---|---:|---:|---|---|
| 使能状态 | `0x01010401` | 2 | DATA0=`M`；DATA1=`0=失能/1=使能` | 上电使能；可用于简单测试机构运动、蜂鸣器鸣叫等 |
| 取天空块 | `0x01010402` | 2 | DATA0=`G`；DATA1=`S` | 把天空块从地上抓起 |
| 放天空块 | `0x01010403` | 2 | DATA0=`P`；DATA1=`S` | 把天空块放到塔顶 |
| 臂复位 | `0x01010404` | 2 | DATA0=`A`；DATA1=`R` | 机械臂回到初位 |
| 爪子开/关 | `0x01010405` | 2 | DATA0=`R`；DATA1=`0=关/1=开` | 解析 DATA1 后控制爪子开关 |
| 复位 | `0x010104FF` | 2 | DATA0=`R`；DATA1=`S` | 执行复位；不需额外回调消息，蜂鸣器回响 |

## 天空块机构从板 -> 主控板

| 功能 | CANID | DLC | 数据区 | 说明 |
|---|---:|---:|---|---|
| 使能状态 | `0x04010101` | 2 | DATA0=`M`；DATA1=`0=失能/1=使能` | 回传天空块机构从板当前使能状态 |
| 机构电机报错 | `0x040101EE` | 待定 | 待定 | 夹人机构电机报错或运动出错时反馈；具体 DLC 和错误字节待协议补充 |
| 复位响应 | `0x040101FF` | 2 | DATA0=`R`；DATA1=`S` | 对复位命令的响应 |

## 从板实现注意事项

1. 从板上电后先等待主控板发送 `0x01010401` 使能命令，未使能时禁止执行机构动作。
2. 从板收到动作命令后，应按 CAN ID 和 DATA 字段校验命令，再驱动机械臂或爪子。
3. 发生电机故障或运动异常时，发送 `0x040101EE`；故障帧的数据格式以协议后续版本为准。
4. 收到 `0x010104FF` 后执行复位，并按约定回传 `0x040101FF`。
